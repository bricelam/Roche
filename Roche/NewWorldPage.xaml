<Page
    x:Class="Roche.NewWorldPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Roche"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Margin="24,0,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="22" VerticalAlignment="Center">
            Create New World
        </TextBlock>
        <StackPanel Margin="0,24,0,0" Grid.Row="1">
            <TextBox Header="World Name" Text="{x:Bind World.WorldName}" />
            <ComboBox Header="Game Mode" Margin="0,24,0,0" SelectedItem="{x:Bind World.GameMode}" />
            <ComboBox Header="World Type" Margin="0,24,0,0" SelectedItem="{x:Bind World.WorldType}" />
            <TextBox Header="Seed" Margin="0,24,0,0" Text="{x:Bind World.Seed}" />
        </StackPanel>
        <StackPanel Grid.Row="2" HorizontalAlignment="Right">
            <Button Style="{StaticResource AccentButtonStyle}" Click="HandleNextClick" Width="120">Next</Button>
        </StackPanel>
    </Grid>
</Page>
